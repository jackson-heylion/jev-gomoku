name: Historical Run3 Audit

on:
  push:
    branches:
      - diag/historical-run3-audit

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22.13.0'
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Run deterministic run3 audit
        run: node benchmark/historical-run3-audit.mjs
